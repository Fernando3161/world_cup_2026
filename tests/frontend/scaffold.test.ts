import { describe, expect, it } from "vitest";

describe("stage 1 frontend scaffold", () => {
  it("keeps the frontend test harness wired", () => {
    expect("static-site").toBe("static-site");
  });
});

